repos:
  - repo: https://github.com/astral-sh/ruff-pre-commit
    rev: v0.9.9
    hooks:
      - id: ruff           # lint + auto-fix
        args: [--fix]
      - id: ruff-format    # formatter (black 대체)
